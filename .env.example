#================================================================
# Base URLs / URL cơ sở
#================================================================
# URL gốc của trang xtruyen
BASE_XTRUYEN=https://xtruyen.vn
# URL gốc của trang tangthuvien
BASE_TANGTHUVIEN=https://tangthuvien.net/tong-hop
# Danh sách các trang web được phép crawl (ví dụ: xtruyen,tangthuvien). Để trống để bật tất cả.
# List of enabled sites (e.g., xtruyen,tangthuvien). Leave empty to enable all.
ENABLED_SITE_KEYS=

#================================================================
# Crawl behaviour / Cấu hình hành vi crawl
#================================================================
# Thời gian chờ (giây) giữa các request.
# Delay (in seconds) between requests.
REQUEST_DELAY=5
# Thời gian tối đa (giây) cho một request trước khi bị timeout.
# Timeout (in seconds) for a single request.
TIMEOUT_REQUEST=30
# Số lần thử lại nếu request thất bại.
# Number of retry attempts on a failed request.
RETRY_ATTEMPTS=3
# Thời gian chờ (giây) trước khi thử lại.
# Delay (in seconds) before retrying.
DELAY_ON_RETRY=2.5
# Số vòng thử lại tối đa cho việc crawl thể loại.
# Maximum retry rounds for crawling genres.
RETRY_GENRE_ROUND_LIMIT=3
# Thời gian ngủ (giây) giữa các vòng thử lại.
# Sleep time (in seconds) between retry rounds.
RETRY_SLEEP_SECONDS=1800
# Số lượt chạy lại để crawl các chương bị lỗi.
# Number of passes to re-crawl failed chapters.
RETRY_FAILED_CHAPTERS_PASSES=2
# Số lượng chương trong một lô xử lý.
# Number of chapters in a processing batch.
NUM_CHAPTER_BATCHES=10
# Số chương tối đa để crawl cho mỗi truyện (0 = không giới hạn).
# Maximum chapters to crawl per story (0 = unlimited).
MAX_CHAPTERS_PER_STORY=0
# Số vòng thử lại tối đa cho việc crawl truyện.
# Maximum retry rounds for crawling stories.
RETRY_STORY_ROUND_LIMIT=40
# Tên file JSON để lưu các truyện đã bỏ qua.
# JSON file to store skipped stories.
SKIPPED_STORIES_FILE=skipped_stories.json
# Số lần thử lại tối đa cho một chương bị lỗi.
# Maximum retries for a single failed chapter.
MAX_CHAPTER_RETRY=3
# Số lượng thể loại tối đa để crawl (0 = không giới hạn).
# Maximum number of genres to crawl (0 = unlimited).
MAX_GENRES_TO_CRAWL=0
# Số truyện tối đa để crawl trên mỗi trang thể loại (0 = không giới hạn).
# Maximum stories to crawl per genre page (0 = unlimited).
MAX_STORIES_PER_GENRE_PAGE=0
# Tổng số truyện tối đa để crawl cho mỗi thể loại (0 = không giới hạn).
# Maximum total stories to crawl per genre (0 = unlimited).
MAX_STORIES_TOTAL_PER_GENRE=0
# Số trang chương tối đa để crawl (0 = không giới hạn).
# Maximum chapter pages to crawl (0 = unlimited).
MAX_CHAPTER_PAGES_TO_CRAWL=0
# Giới hạn số lượng request đồng thời.
# Semaphore limit for concurrent async requests.
ASYNC_SEMAPHORE_LIMIT=5
# Giới hạn số lượng crawl thể loại đồng thời.
# Async limit for genre crawling.
GENRE_ASYNC_LIMIT=3
# Số lượng thể loại trong một lô xử lý đồng thời.
# Number of genres in a concurrent batch.
GENRE_BATCH_SIZE=3
# Giới hạn số lượng crawl truyện đồng thời.
# Async limit for story crawling.
STORY_ASYNC_LIMIT=3
# Số lượng truyện trong một lô xử lý đồng thời.
# Number of stories in a concurrent batch.
STORY_BATCH_SIZE=3
# Kích thước lô xử lý mặc định.
# Default batch size.
BATCH_SIZE=

#================================================================
# Storage paths / Đường dẫn lưu trữ
#================================================================
# Thư mục lưu dữ liệu truyện.
# Folder to store story data.
DATA_FOLDER=truyen_data
# Thư mục lưu các truyện đã hoàn thành.
# Folder to store completed stories.
COMPLETED_FOLDER=completed_stories
# Thư mục lưu trữ bản sao lưu.
# Folder for backups.
BACKUP_FOLDER=backup_truyen_data
# Thư mục lưu trạng thái của ứng dụng.
# Folder to store application state.
STATE_FOLDER=state
# Thư mục lưu file log.
# Folder to store log files.
LOG_FOLDER=logs
# Tên file JSON để lưu các thể loại bị lỗi.
# JSON file to store failed genres.
FAILED_GENRES_FILE=failed_genres.json
# Đường dẫn đến file chứa các mẫu để loại trừ (blacklist).
# Path to the blacklist patterns file.
PATTERN_FILE=config/blacklist_patterns.txt
# Đường dẫn đến file chứa các mẫu để chống bot.
# Path to the anti-bot patterns file.
ANTI_BOT_PATTERN_FILE=config/anti_bot_patterns.txt

#================================================================
# Proxy configuration / Cấu hình proxy
#================================================================
# Bật/tắt sử dụng proxy (true/false).
# Enable/disable proxy usage (true/false).
USE_PROXY=false
# Thư mục chứa các file proxy.
# Folder containing proxy files.
PROXIES_FOLDER=proxies
# File chứa danh sách proxy.
# File containing the proxy list.
PROXIES_FILE=proxies/proxies.txt
# Tên người dùng cho proxy (nếu có).
# Username for the proxy (if any).
PROXY_USER=
# Mật khẩu cho proxy (nếu có).
# Password for the proxy (if any).
PROXY_PASS=
# URL API để lấy proxy động (nếu có).
# API URL for dynamic proxies (if any).
PROXY_API_URL=

#================================================================
# Kafka configuration / Cấu hình Kafka
#================================================================
# Tên topic Kafka để gửi dữ liệu crawl.
# Kafka topic for crawled data.
KAFKA_TOPIC=crawl_truyen
# Địa chỉ các broker Kafka (ví dụ: host1:port1,host2:port2).
# Kafka broker addresses.
KAFKA_BROKERS=kafka:29092
# ID của consumer group.
# Consumer group ID.
KAFKA_GROUP_ID=novel-crawler-group
# Số lần thử lại kết nối bootstrap tối đa (0 = không giới hạn).
# Maximum bootstrap connection retries (0 = unlimited).
KAFKA_BOOTSTRAP_MAX_RETRIES=0
# Thời gian chờ (giây) giữa các lần thử lại kết nối bootstrap.
# Delay (in seconds) between bootstrap connection retries.
KAFKA_BOOTSTRAP_RETRY_DELAY=5
# Tên topic Kafka cho các cảnh báo thiếu dữ liệu.
# Kafka topic for missing data warnings.
MISSING_WARNING_TOPIC=missing_warnings
# ID của consumer group cho các cảnh báo.
# Consumer group ID for warnings.
MISSING_WARNING_GROUP=missing-warning-group

#================================================================
# AI configuration / Cấu hình AI
#================================================================
# Đường dẫn đến file lưu trữ hồ sơ AI.
# Path to the AI profiles file.
AI_PROFILES_PATH=state/ai_profiles.json
# Đường dẫn đến file lưu trữ các chỉ số AI.
# Path to the AI metrics file.
AI_METRICS_PATH=state/ai_metrics.json
# Model AI sẽ sử dụng (ví dụ: gpt-4o-mini).
# AI model to use (e.g., gpt-4o-mini).
AI_MODEL=gpt-4o-mini
# Thời gian sống (giờ) của một hồ sơ AI trước khi được làm mới.
# Time-to-live (in hours) for an AI profile before refresh.
AI_PROFILE_TTL_HOURS=720
# URL base của API OpenAI.
# Base URL for the OpenAI API.
OPENAI_BASE=https://api.openai.com/v1
# Khóa API của OpenAI.
# OpenAI API key.
OPENAI_API_KEY=
# Kích thước tối đa (bytes) để cắt bớt dữ liệu đầu vào cho AI.
# Maximum size (in bytes) to trim AI input data.
AI_TRIM_MAX_BYTES=40000
# In ra các chỉ số AI (true/false).
# Print AI metrics (true/false).
AI_PRINT_METRICS=false

#================================================================
# Telegram / Cấu hình thông báo Telegram
#================================================================
# Token của bot Telegram.
# Telegram bot token.
TELEGRAM_BOT_TOKEN=
# ID của cuộc trò chuyện (chat) hoặc kênh (channel) trên Telegram.
# Telegram chat/channel ID.
TELEGRAM_CHAT_ID=
# ID của chủ đề (thread/topic) trong một nhóm Telegram (tùy chọn).
# Telegram thread/topic ID within a group (optional).
TELEGRAM_THREAD_ID=
# Chế độ phân tích cú pháp tin nhắn (Markdown/HTML).
# Message parse mode (Markdown/HTML).
TELEGRAM_PARSE_MODE=Markdown
# Tắt xem trước trang web trong tin nhắn (true/false).
# Disable web page preview in messages (true/false).
TELEGRAM_DISABLE_WEB_PAGE_PREVIEW=true

#================================================================
# Missing crawl workers / Cấu hình worker crawl dữ liệu thiếu
#================================================================
# Thời gian chờ tối đa (giây) cho worker.
# Timeout (in seconds) for the missing crawl worker.
MISSING_CRAWL_TIMEOUT_SECONDS=60
# Thời gian chờ thêm cho mỗi chương.
# Additional timeout per chapter.
MISSING_CRAWL_TIMEOUT_PER_CHAPTER=4
# Thời gian chờ tối đa cuối cùng.
# Final maximum timeout.
MISSING_CRAWL_TIMEOUT_MAX=900

#================================================================
# CLI defaults / Giá trị mặc định cho CLI
#================================================================
# Chế độ hoạt động mặc định khi chạy từ dòng lệnh.
# Default operation mode when running from CLI.
MODE=
# Chế độ crawl mặc định.
# Default crawl mode.
CRAWL_MODE=